patch:
    schema:
      name: "🀄"

    # 双拼不展开
    translator/preedit_format: []
    # 切换
    switches:
        - name: traditionalization
          states: [简, 繁]
          reset: 0
