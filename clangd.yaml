CompileFlags:
  CompilationDatabase: build/       # Search build/ directory for compile_commands.json
If:
  PathMatch: [".*\\.cc$", ".*\\.hh$"]
CompileFlags:
  Add: [-std=c++17, -Wall]
---
If:
  PathMatch: [".*\\.c$"]
CompileFlags:
  Add: [-std=gnu17, -Wall]
